import express from "express";

const router = express.Router();

// router.get("/", getPresentations);
// router.post("/:nickname/create", createPresentation);
// router.post("/:nickname/join", joinPresentation);
